<!DOCTYPE html>

<title>New Tunnel</title>

{% block body %}
    <h1>Create new Tunnel</h1>

    {{ include('tunnel/_form.html.twig') }}

    <a href="{{ path('app_tunnel_index') }}">back to list</a>
{% endblock %}
