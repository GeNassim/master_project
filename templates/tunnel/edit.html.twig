<!DOCTYPE html>

<title>Edit Tunnel</title>

{% block body %}
    <h1>Edit Tunnel</h1>

    {{ include('tunnel/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tunnel_index') }}">back to list</a>

    {{ include('tunnel/_delete_form.html.twig') }}
{% endblock %}
