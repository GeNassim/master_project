<form method="post" action="{{ path('app_atterissage_delete', {'id': atterissage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ atterissage.id) }}">
    <button class="btn">Delete</button>
</form>
