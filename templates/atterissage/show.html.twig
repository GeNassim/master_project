<!DOCTYPE html>

<title>Atterissage</title>

{% block body %}
    <h1>Atterissage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ atterissage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ atterissage.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ atterissage.url }}</td>
            </tr>
            <tr>
                <th>Visuel</th>
                <td>{{ atterissage.visuel }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ atterissage.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_atterissage_index') }}">back to list</a>

    <a href="{{ path('app_atterissage_edit', {'id': atterissage.id}) }}">edit</a>

    {{ include('atterissage/_delete_form.html.twig') }}
{% endblock %}
