<!DOCTYPE html>

<title>Action</title>

{% block body %}
    <h1>Action</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ action.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_action_index') }}">back to list</a>

    <a href="{{ path('app_action_edit', {'id': action.id}) }}">edit</a>

    {{ include('action/_delete_form.html.twig') }}
{% endblock %}
